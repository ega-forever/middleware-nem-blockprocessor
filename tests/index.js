require('dotenv/config');

const expect = require('chai').expect;

describe('core/block processor', function () {
	before(() => {
		//
	});
	
	after(() => {
		//
	});

	it('do any action', async () => {
		//
	});
});